export type ProfileState = {
    id: string;
    firstName: string;
    lastName: string;
    title: string;
}

export type ProfileResponse = {
    id: string;
    firstName: string;
    lastName: string;
    title: string;
}